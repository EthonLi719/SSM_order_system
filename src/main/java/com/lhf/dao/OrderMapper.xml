<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lhf.dao.OrderMapper">

    <resultMap id="OrderResultMap" type="com.lhf.po.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
    </resultMap>

    <insert id="insert" parameterType="com.lhf.po.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, total_amount, status)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status})
    </insert>

    <update id="update" parameterType="com.lhf.po.Order">
        UPDATE `order`
        <set>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM `order` WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>

    <select id="findAll" resultMap="OrderResultMap">
        SELECT * FROM `order` ORDER BY id DESC
    </select>

    <select id="findByUserId" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE user_id = #{userId} ORDER BY id DESC
    </select>

    <select id="findByStatus" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE status = #{status} ORDER BY id DESC
    </select>

    <select id="findByUserIdAndStatus" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE user_id = #{userId} AND status = #{status} ORDER BY id DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM `order`
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM `order` WHERE user_id = #{userId}
    </select>

    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM `order` WHERE status = #{status}
    </select>

</mapper>